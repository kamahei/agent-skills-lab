# Sample Template

Use this directory as a starting point when creating a new sample under `agent-samples/`.

## How To Use

1. Copy this directory to `agent-samples/<Topic>/`.
2. Replace placeholders in `AGENTS.md`.
3. Update `.env.example` if the sample requires runtime configuration.
4. Add any sample-specific supporting files only if they materially improve reuse.
